name: Publish to WinGet (Disabled)
on:
  # Disabled until proper WINGET_TOKEN is configured and release exists
  # release:
  #   types: [released]
  # workflow_dispatch:

jobs:
  publish:
    if: false  # Disable this workflow temporarily
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "WinGet publishing is disabled."
          echo "To enable:"
          echo "1. Configure WINGET_TOKEN secret"
          echo "2. Create a proper release"
          echo "3. Remove 'if: false' condition"

      # Original winget-releaser step (commented out)
      # - uses: vedantmgoyal9/winget-releaser@main
      #   with:
      #     identifier: RustDesk.RustDesk
      #     version: "1.4.2"
      #     release-tag: "1.4.2"
      #     token: ${{ secrets.WINGET_TOKEN }}